- Prefers consistent design language across pages — when a new layout/header design is introduced (e.g., the editorial warm palette with cream, dark green, and gold), related pages should reuse the same layout and color tokens rather than falling back to an "old" flat design system. Consistency of header, layout, and color scheme across the storefront is valued over page-by-page design divergence. Confidence: 0.9
- Prefers visible input controls over icon-only toggles on mobile — explicitly corrected a search "icon" toggle to an always-visible search bar in the mobile header, indicating a general preference for actionable, discoverable inputs that don't require a tap to reveal.
- Registers requested content seeders in the default `DatabaseSeeder` so the standard `php artisan db:seed` includes them, rather than gating them behind separate `--class`-only or demo-only seed commands. The user explicitly overrode an existing "demo data not automatic" policy to have articles seeded by default, indicating a preference for the default seed command to be comprehensive enough for normal development use. Confidence: 0.85
- On mobile, reserves the bottom nav for primary page navigation only (Beranda, Toko, Pesanan, Profil) — utility/access actions like cart/checkout are accessed exclusively via the header top-right icon and are not duplicated as bottom-nav items, since that duplication is considered unnecessary. Confidence: 0.85
- Avoids duplicating links and controls across the UI — explicitly removed footer nav links (Beranda/Toko/Tentang) because they already exist in the bottom nav bar, and removed an in-page search bar from the catalog filter panel because the header search already covers it, indicating a general preference for each link/control to live in one place rather than being repeated in multiple regions (header, footer, bottom nav, in-page filter panels). Confidence: 0.85
- Prefers left-aligned text/content in layouts rather than centered — explicitly corrected a footer's store info block from `text-center`/`items-center` to `text-left`/`items-start`, indicating a general preference for flush-left alignment over centering in UI layouts. Confidence: 0.75
- Prefers left-aligned decorative elements (dividers, HRs) — explicitly asked to make a divider "sejajar dengan alamat" (aligned with the address) rather than centered, indicating that not just text but also separator lines should align to the left content edge. Confidence: 0.7
- Prefers compact spacing and has a low tolerance for excess whitespace when content is sparse — repeatedly flagged excess padding/margins (e.g., "too wide and there's no content," "copyright section has too much padding/margin"), and the agent tightened `py-8`→`py-6`, `pb-28`→`pb-24`, then `py-3`→`py-2` in response, indicating a preference for dense, efficient layouts over generous spacing and a per-element eye for trimming whitespace. Confidence: 0.8
- Prefers partial-width dividers over full-width separator lines — explicitly asked to make an HR/divider "not full, only half," and the agent replaced a full-width `border-t` with a centered, half-width pseudo-element (`before:w-1/2`), indicating a preference for subtle, centered partial-width separators (e.g., 50% width) rather than edge-to-edge lines for decorative dividers. Confidence: 0.8
- Search/filter inputs trigger requests only on explicit submission — typing is exploratory and should not debounce or autocomplete-as-you-type; Enter keypress submits the search, and category/filter dropdowns auto-apply immediately on change (no debounce timer). Clear/reset controls (e.g. X button) trigger immediate data refresh/fetch without requiring Enter — a dedicated clear button is an explicit final decision that should take effect instantly. Applied consistently across all store pages (Catalog, Promo, header search in Customer/EditorialLayout). Confidence: 0.9
- During active search/filter, hides only promoted-content sections (Featured hero, "Artikel Terbaru") while keeping section titles and category filter controls visible — so users retain context and can combine search with category filtering rather than clearing the entire UI. Confidence: 0.7
- Cart icon only appears on store/catalog pages, not on article or home pages — the header cart is conditionally hidden when the current page context doesn't involve shopping. Confidence: 0.85
- Mobile header displays both a brand icon and the company name (not just the icon) for brand clarity on small screens. Confidence: 0.8
- Default/fallback header logo should be an actual image file placed in `public/` (accessible via root-relative URL like `/logo-pcp.png`), not a generic icon component (e.g., `BookOpen` in a colored box) — user explicitly swapped the `BookOpen` icon fallback in the sticky header for a real `logo-pcp.png` asset. Confidence: 0.9
- Public-facing images and assets should reside in the `public/` directory so they are accessible via root-relative URLs. Confidence: 0.85
- Prefers deterministic ordering for featured/recommended content over randomization — featured lists should keep a stable order (e.g., by `created_at DESC`) rather than `inRandomOrder()`, because randomization changes the displayed items on every page load/visit, confusing returning users who expect a consistent set of featured items. Confidence: 0.8
- References project files and directories with `@`-prefix shorthand in messages (e.g., `@logo-pcp.png`, `@public`) rather than full paths. Confidence: 0.7
- Prefers hybrid auto-scroll with manual override — auto-scrolling content sections (e.g., promo carousels) should auto-advance only when the user is not interacting, and pause on hover/touch to allow manual scrolling; once the user manually scrolls, auto-scroll should pause briefly (a few seconds) then resume automatically — it should not stay stopped permanently, nor fight the user by resuming immediately while they are still scrolling. This means the container must use `overflow-x-auto` (always scrollable) rather than a CSS-animation marquee that locks scroll, because pure CSS `transform`-based marquees with `overflow: hidden` prevent manual user scroll entirely. Confidence: 0.85
- Prefers stacked/overlapping card layouts with reveal-on-scroll — when displaying a collection of cards with more than one item, cards should be stacked from left to right so the leftmost card is fully visible and subsequent cards overlap beneath (like a stack/tumpukan); scrolling to the right progressively reveals each card, as if opening a stack of cards. This is preferred over a flat layout where all cards are equally visible side by side. Confidence: 0.75
